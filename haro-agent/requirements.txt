pyyaml>=6.0
anthropic>=0.40.0
openai>=1.40.0
python-dateutil>=2.9.0
tzdata>=2024.1
flask>=3.0.0
markdown>=3.6

# Production WSGI server for Railway (see Procfile). Linux-only at runtime; installs
# fine on Windows but can't actually run there - local dev keeps using
# `python -m haro_agent.cli serve` instead.
gunicorn>=22.0.0

# Optional, only needed for the production Gmail mailbox adapter (mailbox/gmail.py).
# Not required for local testing with the "local" mailbox source.
# google-api-python-client>=2.140.0
# google-auth-httplib2>=0.2.0
# google-auth-oauthlib>=1.2.0

pytest>=8.0.0
